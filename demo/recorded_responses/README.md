# Recorded responses

This directory is an **explicit offline replay fallback** for competition use.
The UI/API marks replay mode as replay; it must not be presented as a live model call.
Only model output is replayed. Verification, case evolution and retrieval are executed live.
